<Window x:Class="DMTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DMTool"
        mc:Ignorable="d"
        Title="DM Tool - Bilderverwaltung" Height="450" Width="740"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing"
        AllowDrop="True"
        DragEnter="Window_DragEnter"
        DragOver="Window_DragOver"
        Drop="Window_Drop">

    <Grid Margin="0,0,-138,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border x:Name="DropIndicator" Grid.Row="0" Grid.RowSpan="4" 
                Background="#808080FF" Opacity="0" Visibility="Collapsed"
                AllowDrop="True">
            <TextBlock Text="Bilder hier ablegen" FontSize="24" 
                     HorizontalAlignment="Center" VerticalAlignment="Center" 
                     Foreground="White" FontWeight="Bold"/>
        </Border>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Bild hinzufügen" Margin="0,0,10,0" Padding="5" Click="AddImageButton_Click"/>
            <Button Content="Alle Bilder ausblenden" Margin="0,0,10,0" Padding="5" Click="HideAllButton_Click"/>
            <Button Content="Alle Bilder anzeigen" Margin="0,0,10,0" Padding="5" Click="ShowAllButton_Click"/>
            <Button Content="Alle Bilder entfernen" Margin="0,0,10,0" Padding="5" Click="RemoveAllButton_Click"/>
            <Button Content="Fog of War zurücksetzen" Margin="0,0,10,0" Padding="5" Click="ResetFogButton_Click"/>
            <Button Content="Beenden" Margin="0,0,10,0" Padding="5" Click="ExitButton_Click"/>
        </StackPanel>

        <GroupBox Grid.Row="1" Header="Einstellungen" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Erste Zeile: Label links, Slider+TextBlock rechts -->
                <Label Grid.Row="0" Grid.Column="0" 
               Content="Maximale Bildgröße (px):" 
               VerticalAlignment="Center" 
               Margin="0,5,10,5"/>

                <StackPanel Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" 
                    VerticalAlignment="Center" 
                    Margin="0,5">
                    <Slider x:Name="ImageSizeSlider" 
                    Minimum="100" Maximum="1000" Width="200"
                    Value="{Binding Source={x:Static local:App.AppSettings}, Path=MaxImageSize, Mode=TwoWay}"/>
                    <TextBlock Text="{Binding ElementName=ImageSizeSlider, Path=Value, StringFormat=\{0:F0\}}" 
                       VerticalAlignment="Center" 
                       Margin="10,0,0,0"/>
                </StackPanel>

                <!-- Zweite Zeile: Checkbox links, Checkbox rechts -->
                <CheckBox Grid.Row="1" Grid.Column="0" 
                  Content="Kontextmenü 'Sende an DM-Tool' installieren"
                  IsChecked="{Binding Source={x:Static local:App.AppSettings}, Path=InstallContextMenu, Mode=TwoWay}"
                  Margin="0,5"
                  Checked="ContextMenu_CheckedChanged"
                  Unchecked="ContextMenu_CheckedChanged" />

                <CheckBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" 
                  Content="Debug-Informationen im Overlay anzeigen"
                  IsChecked="{Binding Source={x:Static local:App.AppSettings}, Path=ShowDebugInfo, Mode=TwoWay}"
                  Margin="5,5" />

                <!-- Dritte Zeile: Checkbox links, Label mitte, Slider rechts -->
                <CheckBox Grid.Row="2" Grid.Column="0" 
                  Content="Fog of War aktivieren"
                  IsChecked="{Binding Source={x:Static local:App.AppSettings}, Path=EnableFogOfWar, Mode=TwoWay}"
                  Margin="0,5" />

                <Label Grid.Row="2" Grid.Column="1" 
               Content="Aufdeckungsgröße:" 
               VerticalAlignment="Center" 
               Margin="0,5,10,5" />

                <StackPanel Grid.Row="2" Grid.Column="2" 
                    Orientation="Horizontal" 
                    VerticalAlignment="Center" 
                    Margin="0,5">
                    <Slider x:Name="FogRevealSizeSlider" 
                    Minimum="10" Maximum="200" Width="200"
                    Value="{Binding Source={x:Static local:App.AppSettings}, Path=FogRevealSize, Mode=TwoWay}" />
                    <TextBlock Text="{Binding ElementName=FogRevealSizeSlider, Path=Value, StringFormat=\{0:F0\}px}" 
                       VerticalAlignment="Center" 
                       Margin="10,0,0,0" />
                </StackPanel>

                <!-- Vierte Zeile: Positionsoffsets -->
                <StackPanel Grid.Row="3" Grid.Column="0" 
                    Orientation="Horizontal" 
                    VerticalAlignment="Center" 
                    Margin="0,5">
                    <Label Content="Positionskorrektur:" VerticalAlignment="Center" />
                </StackPanel>

                <Grid Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" 
              VerticalAlignment="Center" 
              Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Column="0" Content="X-Offset:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Slider x:Name="PositionOffsetXSlider" 
                        Minimum="-500" Maximum="500" Width="100"
                        Value="{Binding Source={x:Static local:App.AppSettings}, Path=PositionOffsetX, Mode=TwoWay}" 
                        TickFrequency="50" IsSnapToTickEnabled="True"/>
                        <TextBlock Text="{Binding ElementName=PositionOffsetXSlider, Path=Value, StringFormat=\{0:F0\}px}" 
                           VerticalAlignment="Center" 
                           Margin="5,0,0,0" Width="45"/>
                    </StackPanel>

                    <Label Grid.Column="2" Content="Y-Offset:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                    <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                        <Slider x:Name="PositionOffsetYSlider" 
                        Minimum="-500" Maximum="500" Width="100"
                        Value="{Binding Source={x:Static local:App.AppSettings}, Path=PositionOffsetY, Mode=TwoWay}" 
                        TickFrequency="50" IsSnapToTickEnabled="True"/>
                        <TextBlock Text="{Binding ElementName=PositionOffsetYSlider, Path=Value, StringFormat=\{0:F0\}px}" 
                           VerticalAlignment="Center" 
                           Margin="5,0,0,0" Width="45"/>
                    </StackPanel>
                </Grid>

                <!-- Fünfte Zeile: Zielbildschirm -->
                <Label Grid.Row="4" Grid.Column="0" Content="Zielbildschirm:" VerticalAlignment="Center" Margin="0,5,10,5"/>
                <ComboBox Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" 
                          x:Name="ScreenSelector" 
                          Margin="0,5" 
                          SelectionChanged="ScreenSelector_SelectionChanged"
                          DisplayMemberPath="Name"
                          SelectedValuePath="Index"/>
            </Grid>
        </GroupBox>

        <ListView Grid.Row="2" Margin="10" ItemsSource="{Binding Source={x:Static local:App.Images}}" 
                  x:Name="ImagesListView">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Dateiname" Width="160">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding FileName}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Sichtbar" Width="60">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsVisible, Mode=TwoWay}" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Position" Width="140">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock>
                        <Run Text="X:"/>
                        <Run Text="{Binding PosX, Mode=OneWay}"/>
                        <Run Text=" Y:"/>
                        <Run Text="{Binding PosY, Mode=OneWay}"/>
                        <Run Text=" R:"/>
                        <Run Text="{Binding Rotation, StringFormat=\{0:F1\}, Mode=OneWay}"/>
                                </TextBlock>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Aktionen" Width="240">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Neu positionieren" Margin="0,0,5,0" 
                                            Click="RepositionImageButton_Click" 
                                            Tag="{Binding}" 
                                            ToolTip="Bild an neuer zufälliger Position anzeigen"/>
                                    <Button Content="Entfernen" Margin="0,0,5,0" 
                                            Click="RemoveImageButton_Click" 
                                            Tag="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Bereit"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>